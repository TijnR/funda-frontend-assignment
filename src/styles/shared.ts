/** Horizontal page gutters, shared by every top-level <main>. */
export const PAGE_SHELL_CLASS = "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8";

/**
 * Shared by the real listing grid and its skeleton so the two cannot drift apart.
 * The gap is mirrored by the featured ListingCard image width; keep the two in sync.
 */
export const LISTING_GRID_CLASS = "grid list-none gap-4 md:grid-cols-2";
